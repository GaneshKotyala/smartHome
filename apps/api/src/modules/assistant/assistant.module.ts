import { Module } from '@nestjs/common';

// TODO Phase 3: Implement AssistantModule
// - AssistantController (POST /chat, GET /history)
// - AssistantService (Gemini API integration)
// - LanguageDetector (EN / Telugu detection)
// - IntentParser (extracts device/routine commands)
// - ActionDispatcher (calls DevicesService / RoutinesService)
// - prompts/ (system prompt templates)
@Module({})
export class AssistantModule {}
